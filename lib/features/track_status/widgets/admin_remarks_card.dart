import 'package:flutter/material.dart';

class AdminRemarksCard extends StatelessWidget {
  const AdminRemarksCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

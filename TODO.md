# Flutter Hostel Hive Error Fix Progress

## Plan Steps:
1. [ ] Fix pubspec.yaml (assets path, fonts).
2. [ ] Update lib/widgets/custom_image_view.dart (imports, size utils).
3. [ ] Update lib/widgets/custom_text_form_field.dart (imports, size utils).
4. [ ] Polish lib/core/network/network_call.dart (error toasts).
5. [ ] Global search-replace 'book_store' -> 'hostel_hive' in other files.
6. [ ] Run `flutter pub get && flutter analyze`.
7. [ ] Test with `flutter run`.

**Next step: pubspec.yaml**

